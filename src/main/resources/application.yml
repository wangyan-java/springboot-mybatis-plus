server:
  port: 8080
spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      username: root
      password: root
      url: jdbc:mysql://127.0.0.1:3306/mybatis-plus?serverTimezone=UTC&allowMultiQueries=true&autoReconnect=true&characterEncoding=utf-8
      db-type: com.alibaba.druid.pool.DruidDataSource

mybatis-plus:
  mapper-locations: classpath:/mappers/*Mapper.xml
#  type-aliases-package: com.wy.demo.entity